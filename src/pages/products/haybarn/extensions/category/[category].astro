---
import Directory from '../../../../../components/haybarn-extensions/Directory.astro';
import { categories } from '../../../../../data/haybarn-extensions';
export function getStaticPaths() { return categories.map(category => ({ params: { category: category.id } })); }
---
<Directory category={Astro.params.category} />
