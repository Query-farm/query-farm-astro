---
import Function from '@haybarn/views/Function.astro';
import { defaultSnapshot, getFunctions } from '@haybarn/lib/catalog';
export function getStaticPaths() { return getFunctions(defaultSnapshot).map(fn => ({ params: { slug: fn.slug }, props: { snapshot: defaultSnapshot, fn } })); }
---
<Function {...Astro.props} />
