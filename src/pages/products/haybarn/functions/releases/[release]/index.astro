---
import View from '@haybarn/views/Catalog.astro';
import { snapshots } from '@haybarn/lib/catalog';
export function getStaticPaths() { return snapshots.map(snapshot => ({ params: { release: snapshot.id }, props: { snapshot } })); }
---
<View {...Astro.props} />
