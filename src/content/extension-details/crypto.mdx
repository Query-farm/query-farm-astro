---
title: "Crypto Extension Technical Details"
description: "In-depth technical documentation for the crypto extension"
---

import Callout from '../../components/docs/Callout.astro';
import PlotlyChart from '../../components/docs/PlotlyChart.astro';
import D2Diagram from '../../components/docs/D2Diagram.astro';

## Algorithm Support

The crypto extension supports a wide range of cryptographic algorithms for different use cases. Below is a summary of the supported algorithms and their characteristics.

### Hash Functions

| Algorithm | Output Size | Security Level | Use Case |
|-----------|-------------|----------------|----------|
| MD5 | 128 bits | Legacy | Checksums only (not cryptographically secure) |
| SHA-1 | 160 bits | Legacy | Compatibility with older systems |
| SHA-256 | 256 bits | Strong | General purpose hashing |
| SHA-512 | 512 bits | Strong | High security applications |
| BLAKE3 | 256 bits | Strong | High performance hashing |

<Callout type="warning" title="Deprecation Notice">
  MD5 and SHA-1 are provided for compatibility only. Do not use them for security-sensitive applications. Use SHA-256 or BLAKE3 for new projects.
</Callout>

### Encryption Algorithms

| Algorithm | Key Size | Mode | Performance |
|-----------|----------|------|-------------|
| AES-128 | 128 bits | GCM | Fast |
| AES-256 | 256 bits | GCM | Moderate |
| ChaCha20-Poly1305 | 256 bits | AEAD | Very Fast |

### Algorithm Usage Distribution

Based on typical workloads, here's how different algorithms are used in production:

<PlotlyChart
  id="usage-distribution"
  height="300px"
  data={[
    {
      type: 'pie',
      labels: ['SHA-256', 'BLAKE3', 'AES-256-GCM', 'bcrypt', 'Other'],
      values: [35, 25, 20, 15, 5],
      hole: 0.4,
      marker: {
        colors: ['#22c55e', '#66bb6a', '#f59e0b', '#3b82f6', '#d3cab9']
      },
      textinfo: 'label+percent',
      textposition: 'outside',
      hovertemplate: '%{label}: %{percent}<extra></extra>'
    }
  ]}
  layout={{
    title: { text: 'Algorithm Usage in Production', font: { size: 16 } },
    showlegend: false,
    annotations: [{
      text: 'Usage',
      x: 0.5,
      y: 0.5,
      font: { size: 14, color: '#3d342a' },
      showarrow: false
    }]
  }}
/>

## Performance Benchmarks

The following benchmarks were performed on a dataset of 1 million rows using DuckDB's vectorized execution engine.

```sql
-- Benchmark: Hash 1M rows
SELECT COUNT(*) FROM (
    SELECT crypto_sha256(data) as hash
    FROM test_data
);
-- Result: ~450ms for 1M rows
```

### Throughput Comparison

| Operation | Rows/Second | MB/Second |
|-----------|-------------|-----------|
| SHA-256 | 2.2M | 180 |
| BLAKE3 | 4.5M | 360 |
| AES-256-GCM Encrypt | 1.1M | 90 |
| AES-256-GCM Decrypt | 1.2M | 95 |

<PlotlyChart
  id="throughput-chart"
  height="350px"
  data={[
    {
      type: 'bar',
      orientation: 'h',
      y: ['AES-256 Encrypt', 'AES-256 Decrypt', 'SHA-256', 'BLAKE3'],
      x: [1.1, 1.2, 2.2, 4.5],
      text: ['1.1M', '1.2M', '2.2M', '4.5M'],
      textposition: 'outside',
      marker: {
        color: ['#f59e0b', '#fbbf24', '#66bb6a', '#22c55e']
      },
      hovertemplate: '%{y}: %{x}M rows/sec<extra></extra>'
    }
  ]}
  layout={{
    title: { text: 'Throughput (Million Rows/Second)', font: { size: 16 } },
    xaxis: {
      title: 'Rows/Second (Millions)',
      gridcolor: '#e8e4df',
      range: [0, 5.5]
    },
    yaxis: {
      automargin: true
    },
    showlegend: false,
    hoverlabel: {
      bgcolor: '#3d342a',
      font: { color: 'white' }
    }
  }}
/>

## Architecture

The crypto extension integrates seamlessly with DuckDB's query execution pipeline, leveraging vectorized processing for maximum performance.

![Crypto Extension Architecture](/images/extensions/crypto-architecture.svg)

### Decision Flow: Choosing the Right Algorithm

<D2Diagram
  title="Algorithm Selection Guide"
  code={`
direction: right

start: {
  shape: oval
  label: "What do you need?"
}

hash_q: {
  shape: diamond
  label: "One-way hashing?"
}

encrypt_q: {
  shape: diamond
  label: "Need to decrypt?"
}

password_q: {
  shape: diamond
  label: "Password storage?"
}

performance_q: {
  shape: diamond
  label: "High performance?"
}

sha256: {
  shape: hexagon
  label: "SHA-256"
  style.fill: "#dcfce7"
  style.stroke: "#22c55e"
}

blake3: {
  shape: hexagon
  label: "BLAKE3"
  style.fill: "#dcfce7"
  style.stroke: "#22c55e"
}

bcrypt: {
  shape: hexagon
  label: "bcrypt"
  style.fill: "#dbeafe"
  style.stroke: "#3b82f6"
}

aes: {
  shape: hexagon
  label: "AES-256-GCM"
  style.fill: "#fef3c7"
  style.stroke: "#f59e0b"
}

start -> hash_q
hash_q -> encrypt_q: No
hash_q -> password_q: Yes
password_q -> bcrypt: Yes
password_q -> performance_q: No
performance_q -> blake3: Yes
performance_q -> sha256: No
encrypt_q -> aes: Yes
`}
/>

## Implementation Details

### Vectorized Processing

All cryptographic operations in this extension are fully vectorized, meaning they process data in batches of ~2000 rows at a time. This approach maximizes CPU cache utilization and enables SIMD optimizations.

<Callout type="info">
  Vectorized execution is automatic - you don't need to do anything special to take advantage of it. Just write your SQL queries as usual.
</Callout>

```sql
-- Example: Batch hashing with salt
SELECT
    id,
    crypto_sha256(CONCAT(salt, ':', password)) as password_hash
FROM users;
```

### Memory Management

The extension uses DuckDB's native memory allocator and follows these principles:

1. **Zero-copy operations** - Input data is processed in-place when possible
2. **Buffer reuse** - Intermediate buffers are reused across operations
3. **Streaming support** - Large files are processed in chunks

## Security Considerations

When using cryptographic functions in a database context, consider the following:

- **Key Management**: Never store encryption keys in the same database as encrypted data
- **Salt Generation**: Always use cryptographically secure random salts
- **Algorithm Selection**: Use SHA-256 or better for new applications

<Callout type="danger">
  Never hardcode encryption keys or passwords in your SQL queries. Use DuckDB secrets or environment variables for sensitive credentials.
</Callout>

```sql
-- Secure password hashing example
SELECT crypto_bcrypt_hash(
    password,
    crypto_random_bytes(16),  -- Random salt
    12                         -- Work factor
) as secure_hash
FROM new_users;
```

<Callout type="tip">
  For bcrypt, a work factor of 12 provides a good balance between security and performance. Increase it for highly sensitive applications, but test the impact on query time.
</Callout>
