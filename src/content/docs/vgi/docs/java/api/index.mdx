---
title: Package overview
description: "Orientation for farm.query:vgi — the five function shapes, the annotation vocabulary, and the map of every reference page."
---

import Callout from '../../../../../../components/docs/Callout.astro';
import CodeBlock from '../../../../../../components/ui/CodeBlock.astro';
import { CardGrid, LinkCard } from '@astrojs/starlight/components';

One artifact, published to Maven Central as `farm.query:vgi`. Everything below is generated from the
sources, so a type's page is the authority for its exact shape.

<CodeBlock code={`implementation("farm.query:vgi:0.27.0")`} lang="kotlin" tryable={false} />

## The five shapes

<CodeBlock code={`farm.query.vgi.scalar.ScalarFn                        // 1 row  → 1 value
farm.query.vgi.table.TableFunction                    // args   → N rows
farm.query.vgi.tableinout.TableInOutFunction          // N rows → M rows, streaming
farm.query.vgi.aggregate.AggregateFunction            // N rows → 1 value per group
farm.query.vgi.buffering.TableBufferingFunction       // N rows → M rows, after all input`} lang="java" tryable={false} />

Each is registered on the `Worker` builder with the matching `register*` call.

<Callout type="note" title="`-parameters` is part of the contract">
The scalar API derives its SQL signature from your **parameter names**, which Java erases by default.
A build without `-parameters` produces arguments called `arg0` and still works positionally, so
nothing fails visibly. See [Running a worker](/vgi/docs/java/how-to/running/).
</Callout>

## Reference

<CardGrid>
  <LinkCard title="Scalar functions" href="/vgi/docs/java/api/scalar/" description="ScalarFn and the parameter annotations." />
  <LinkCard title="Table functions" href="/vgi/docs/java/api/table/" description="Producers, and the countdown base for generators." />
  <LinkCard title="Table-in-out functions" href="/vgi/docs/java/api/table-in-out/" description="Streaming a relation through." />
  <LinkCard title="Buffering functions" href="/vgi/docs/java/api/buffering/" description="Sink, combine, source." />
  <LinkCard title="Aggregate functions" href="/vgi/docs/java/api/aggregate/" description="Per-group accumulation with cross-process combine." />
  <LinkCard title="Worker & serving" href="/vgi/docs/java/api/worker/" description="The builder, the transports, secrets and settings." />
  <LinkCard title="Catalogs" href="/vgi/docs/java/api/catalog/" description="Schemas, tables, views, macros, versioning." />
  <LinkCard title="Function metadata & params" href="/vgi/docs/java/api/function/" description="The shared bind and process parameter types." />
  <LinkCard title="State storage" href="/vgi/docs/java/api/storage/" description="State across calls and across processes." />
  <LinkCard title="Cache control" href="/vgi/docs/java/api/cache-control/" description="Advertising a result as reusable." />
  <LinkCard title="Filter pushdown" href="/vgi/docs/java/api/pushdown/" description="Predicates pushed toward the scan, and applying them." />
  <LinkCard title="Arrow helpers" href="/vgi/docs/java/api/types/" description="Schemas, per-type scalar helpers, type rules." />
  <LinkCard title="Client" href="/vgi/docs/java/api/client/" description="Calling a worker from Java." />
  <LinkCard title="Protocol" href="/vgi/docs/java/api/protocol/" description="The wire shapes worker and engine exchange." />
</CardGrid>
