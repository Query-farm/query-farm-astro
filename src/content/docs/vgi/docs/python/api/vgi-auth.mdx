---
title: vgi.auth
description: API reference for vgi.auth
---
import { Code } from '@astrojs/starlight/components';

<div class="api-module-doc">

<p class="api-module-doc__label">Module overview</p>

Convenience re-exports of authentication types from vgi-rpc.

Core types (always available):

<Code lang="python" code={`AuthContext, CallContext`} />

HTTP auth factories (require ``vgi[http]``):

<Code lang="python" code={`bearer_authenticate, bearer_authenticate_static, chain_authenticate,
OAuthResourceMetadata, AuthUnavailableError`} />

Token introspection (requires ``vgi[http]``):

<Code lang="python" code={`TokenIdentity, TokenResolver`} />

JWT auth (requires ``vgi[oauth]``):

<Code lang="python" code={`jwt_authenticate`} />



</div>
