---
title: VGI Documentation
description: "Extend DuckDB with out-of-process functions written in Python, Go, TypeScript, Rust, Java, or C#. Start with the shared Concepts, then pick your SDK."
tableOfContents: false
---

import DocsHome from '../../../../components/starlight/DocsHome.astro';

**VGI** — the Vector Gateway Interface — lets you add functions, tables and catalogs to DuckDB from an
ordinary separate program, in whatever language you like. No native extension to compile, no DuckDB
release cadence to wait on. Data crosses the boundary as Apache Arrow record batches.

Every SDK implements the same protocol, so the **Concepts** pages are written once and shared. Read
them first if you want the model before the syntax; otherwise jump straight into a language.

<DocsHome />

Still deciding? **[Language SDKs](/vgi/languages)** compares them side by side — install command,
feature parity, repository, and the RPC layer underneath each one.

## Elsewhere on the site

- **[VGI overview](/vgi)** — what it is and who it's for.
- **[Architecture](/vgi/architecture)** — the engineering case: why out-of-process, what it costs in
  latency, how it compares to native C++ extensions, Airport and ADBC.
- **[Getting started](/vgi/getting-started)** — install the extension and attach your first worker.
- **[vgi-rpc](https://vgi-rpc.query.farm/)** — the transport-agnostic RPC layer underneath, and its
  [wire-protocol specification](https://vgi-rpc.query.farm/wire-protocol).
