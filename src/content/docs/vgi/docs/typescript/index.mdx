---
title: Vector Gateway Interface for TypeScript
description: Build DuckDB scalar, table, and aggregate functions in TypeScript on Apache Arrow — the official documentation for the @query-farm/vgi package.
---

import { CardGrid, LinkCard, LinkButton } from '@astrojs/starlight/components';
import CodeBlock from '../../../../../components/ui/CodeBlock.astro';
import GitHubStar from '../../../../../components/GitHubStar.astro';

<div class="vgi-hero">
<picture><source srcset="/vgi/vgi-emblem.webp" type="image/webp" /><img src="/vgi/vgi-emblem.png" alt="Vector Gateway Interface" class="vgi-hero__logo" width="800" height="588" fetchpriority="high" /></picture>
<p class="vgi-hero__tagline">Extend DuckDB with functions written in TypeScript — backed by Apache&nbsp;Arrow, callable straight from SQL.</p>
</div>

<div class="vgi-hero__links">
<LinkButton href="/vgi/docs/typescript/tutorial/scalar/" icon="rocket">Get started</LinkButton>
<LinkButton href="https://github.com/Query-farm/vgi-typescript" variant="secondary" icon="github">GitHub repository</LinkButton>
</div>

<div class="vgi-hero__star"><GitHubStar repo="Query-farm/vgi-typescript" /></div>

**`@query-farm/vgi`** implements the Vector Gateway Interface (VGI) for TypeScript: you write a small
worker exposing typed functions, and DuckDB calls them as if they were native. Data moves as Apache
Arrow record batches, so it stays columnar across the boundary — no row-by-row marshalling.

## Install

<CodeBlock code={`bun add @query-farm/vgi @query-farm/apache-arrow @query-farm/vgi-rpc`} lang="bash" tryable={false} />

All three go in explicitly: the latter two are **peerDependencies**, so a single shared copy is used
rather than two that disagree. Requires Node.js 22.15+ or Bun.

A worker is an ordinary module. Define functions, hand them to a `Worker`, and run it:

<CodeBlock code={`import { Worker, defineScalarFunction, int } from "@query-farm/vgi";

const double = defineScalarFunction({
  name: "double",
  params: { n: int },
  returns: int,
  compute: (batch) => {
    const ns = batch.getChildAt(0)! as Iterable<bigint | null>;
    return Array.from(ns, (v) => (v == null ? null : v * 2n));
  },
});

new Worker({
  catalog: { name: "calc", schemas: [{ name: "main", functions: [double] }] },
}).run();`} lang="typescript" tryable={false} />

<CodeBlock code={`ATTACH 'calc' (TYPE vgi, LOCATION 'bun run ./worker.ts');
SELECT calc.double(21);   -- 42`} lang="sql" tryable={false} />

`LOCATION` is the **command** DuckDB runs, not a path to a binary — which is why a TypeScript worker
needs no build step at all.

## Start here

<CardGrid>
  <LinkCard
    title="Tutorial"
    href="/vgi/docs/typescript/tutorial/scalar/"
    description="Zero to a scalar and a table function callable from DuckDB, in about 20 minutes."
  />
  <LinkCard
    title="Concepts"
    href="/vgi/docs/concepts/"
    description="What VGI is, the worker model, and the Arrow data path — shared across every SDK."
  />
  <LinkCard
    title="API reference"
    href="/vgi/docs/typescript/api/"
    description="The whole package — function factories, worker, catalogs, client — generated from the source."
  />
  <LinkCard
    title="Function lifecycle"
    href="/vgi/docs/concepts/lifecycle/"
    description="When bind, init, process, combine and finalize each run, and how they map to DuckDB."
  />
</CardGrid>

## Three things to know up front

- **Functions are declared, not subclassed.** `defineScalarFunction({ … })` and its four siblings take
  a config object. There is no base class to extend and no interface to satisfy — the config *is* the
  function, and its `params` are the SQL signature.
- **Columns are erased.** `batch.getChildAt(i)` yields `unknown` values, because the SDK runs on two
  different Arrow backends. Cast at the use site, and go through `iterRows` for dates and decimals —
  see [Value representations](/vgi/docs/typescript/how-to/value-representations/).
- **It runs where the engine runs.** The same functions serve from Node or Bun, from a Cloudflare
  Worker at the edge, or behind an HTTP endpoint a browser talks to. See
  [Runtimes](/vgi/docs/typescript/how-to/runtimes/).

Coming from another SDK? The protocol is identical and a TypeScript worker is wire-compatible with a
Python, Go or Rust one — the same DuckDB extension drives all of them, so the concepts carry over
unchanged.

## Reference

<CardGrid>
  <LinkCard
    title="Package overview"
    href="/vgi/docs/typescript/api/"
    description="Orientation for the package, plus the map of every reference page."
  />
  <LinkCard
    title="Argument serialization"
    href="/vgi/docs/concepts/argument-serialization/"
    description="The Arrow schema every SDK produces for a function signature — the shared wire format."
  />
</CardGrid>
