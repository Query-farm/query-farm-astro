---
title: Vector Gateway Interface for Go
description: Build DuckDB scalar, table, and aggregate functions in Go on Apache Arrow — the official documentation for the vgi-go module.
---

import { CardGrid, LinkCard, LinkButton } from '@astrojs/starlight/components';
import CodeBlock from '../../../../../components/ui/CodeBlock.astro';
import GitHubStar from '../../../../../components/GitHubStar.astro';

<div class="vgi-hero">
<picture><source srcset="/vgi/vgi-emblem.webp" type="image/webp" /><img src="/vgi/vgi-emblem.png" alt="Vector Gateway Interface" class="vgi-hero__logo" width="800" height="588" fetchpriority="high" /></picture>
<p class="vgi-hero__tagline">Extend DuckDB with functions written in Go — backed by Apache&nbsp;Arrow, callable straight from SQL.</p>
</div>

<div class="vgi-hero__links">
<LinkButton href="/vgi/docs/go/tutorial/scalar/" icon="rocket">Get started</LinkButton>
<LinkButton href="https://github.com/Query-farm/vgi-go" variant="secondary" icon="github">GitHub repository</LinkButton>
</div>

<div class="vgi-hero__star"><GitHubStar repo="Query-farm/vgi-go" /></div>

**vgi-go** implements the Vector Gateway Interface (VGI) for Go: you write a small worker exposing
typed functions, and DuckDB calls them as if they were native. Data moves as Apache Arrow record
batches on [arrow-go](https://github.com/apache/arrow-go), so it stays columnar across the boundary —
no row-by-row marshalling.

## Install

<CodeBlock code={`go get github.com/Query-farm/vgi-go`} lang="bash" tryable={false} />

The module path is `github.com/Query-farm/vgi-go`; the package you import is
`github.com/Query-farm/vgi-go/vgi`.

A worker is an ordinary `main` package. Register your functions on a `Worker`, then pick a transport:

<CodeBlock code={`import "github.com/Query-farm/vgi-go/vgi"

// DoubleFn and doubleArgs are yours — the tutorial builds them in ten minutes.
w := vgi.NewWorker(vgi.WithCatalogName("calc"))
w.RegisterScalar(vgi.AsScalarFunction[doubleArgs](&DoubleFn{}))
w.RunStdio()   // or w.RunHttp(addr) / w.RunUnix(path, idle) / w.RunTcp(host, port, idle)`} lang="go" tryable={false} />

## Start here

<CardGrid>
  <LinkCard
    title="Tutorial"
    href="/vgi/docs/go/tutorial/scalar/"
    description="Zero to a scalar and a table function callable from DuckDB, in about 20 minutes."
  />
  <LinkCard
    title="Concepts"
    href="/vgi/docs/concepts/"
    description="What VGI is, the worker model, and the Arrow data path — shared across every SDK."
  />
  <LinkCard
    title="API reference"
    href="/vgi/docs/go/api/"
    description="The full vgi package — function shapes, worker, catalogs, storage — generated from the source."
  />
  <LinkCard
    title="Function lifecycle"
    href="/vgi/docs/concepts/lifecycle/"
    description="When OnBind, OnInit, Process, Combine and Finalize each run, and how they map to DuckDB."
  />
</CardGrid>

## Two things to know up front

- **Arguments are declared with struct tags.** A `vgi:"..."` tag on a field drives both the argument
  spec the catalog advertises and the runtime binding of values into your struct, so the SQL
  signature and the Go type cannot drift apart. The full grammar is on
  [Argument tags](/vgi/docs/go/how-to/argument-tags/).
- **You implement an interface, not a base class.** `ScalarFunction`, `TableFunction`,
  `TableInOutFunction`, `TableBufferingFunction` and `AggregateFunction` are interfaces. The `Typed*`
  variants (`TypedScalarFunc`, `TypedTableFunc`) add the struct-tag ergonomics, and
  `AsScalarFunction` / `AsTableFunction` adapt one into the other. Optional hooks — `OnInit`,
  `Statistics`, `Cardinality` — are separate small interfaces you satisfy only if you want them; see
  [Function patterns](/vgi/docs/go/how-to/function-patterns/#parallelism-and-oninit).

Coming from another SDK? The protocol is identical and a Go worker is wire-compatible with a Python
or Rust one — the same DuckDB extension drives all of them, so the concepts carry over unchanged.

## Reference

<CardGrid>
  <LinkCard
    title="Package overview"
    href="/vgi/docs/go/api/"
    description="Orientation for the vgi package, plus the map of every reference page."
  />
  <LinkCard
    title="Argument serialization"
    href="/vgi/docs/concepts/argument-serialization/"
    description="The Arrow schema every SDK produces for a function signature — the shared wire format."
  />
</CardGrid>
