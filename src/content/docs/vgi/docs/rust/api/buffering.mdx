---
title: "Buffering functions"
description: "Sink, combine, source — for output that depends on the whole input, via TableBufferingFunction."
---
import { Code } from '@astrojs/starlight/components';

<div class="api-module-doc">

<p class="api-module-doc__label">On this page</p>

Sink, combine, source — for output that depends on the whole input, via TableBufferingFunction.

</div>

<div class="api-member">

<a id="TableBufferingFunction"></a>
## <span class="api-icon api-icon--class"></span><span class="api-kind-tag api-kind-tag--class">trait</span> `TableBufferingFunction`

<a class="api-source" href="https://github.com/Query-farm/vgi-rust/blob/main/vgi/src/buffering.rs#L73" target="_blank" rel="noopener">source</a>

<Code lang="rust" code={`pub trait TableBufferingFunction: Send + Sync {
    fn name(&self) -> &str;
    fn metadata(&self) -> FunctionMetadata;
    fn argument_specs(&self) -> Vec<ArgSpec>;
    fn secret_lookups(&self, _params: &BindParams) -> Vec<crate::secrets::SecretLookup> {
        Vec::new()
    }
    fn on_bind(&self, params: &BindParams) -> Result<BindResponse>;
    fn process(
        &self,
        params: &BufferingParams,
        batch: &arrow_array::RecordBatch,
    ) -> Result<Vec<u8>>;
    fn combine(&self, params: &BufferingParams, state_ids: &[Vec<u8>]) -> Result<Vec<Vec<u8>>>;
    fn finalize_producer(
        &self,
        params: &BufferingParams,
        finalize_state_id: Vec<u8>,
    ) -> Result<Box<dyn TableProducer>>;
}`} />

<p class="api-section">Description</p>

A table buffering (sink+source) function.

</div>

<div class="api-member">

<a id="BufferingParams"></a>
## <span class="api-icon api-icon--class"></span><span class="api-kind-tag api-kind-tag--class">struct</span> `BufferingParams`

<a class="api-source" href="https://github.com/Query-farm/vgi-rust/blob/main/vgi/src/buffering.rs#L29" target="_blank" rel="noopener">source</a>

<Code lang="rust" code={`pub struct BufferingParams {
    pub execution_id: Vec<u8>,
    pub storage: Arc<dyn FunctionStorage>,
    pub output_schema: SchemaRef,
    pub arguments: Arguments,
    pub settings: Settings,
    pub secrets: crate::secrets::Secrets,
    pub attach_opaque_data: Option<Vec<u8>>,
    pub batch_index: Option<i64>,
    pub copy_to: Option<crate::protocol::dtos::CopyToContext>,
    pub input_schema: Option<SchemaRef>,
    pub logs: Arc<std::sync::Mutex<Vec<String>>>,
}`} />

<p class="api-section">Description</p>

Parameters for buffering process / combine / finalize.

<p class="api-section">Methods</p>

<div class="api-members">

<div class="api-member">

<a id="BufferingParams.log"></a>
### <span class="api-icon api-icon--method"></span><span class="api-kind-tag api-kind-tag--method">method</span> `log`

<a class="api-source" href="https://github.com/Query-farm/vgi-rust/blob/main/vgi/src/buffering.rs#L65" target="_blank" rel="noopener">source</a>

<pre class="api-sig"><code>pub fn log(&amp;self, message: impl Into&lt;String&gt;)</code></pre>

Queue an INFO-level client log line (surfaced under `duckdb_logs()`).

</div>
</div>

</div>
