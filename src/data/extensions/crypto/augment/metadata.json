{
  "name": "crypto",
  "displayName": "Crypto",
  "icon": "🔐",
  "description": "Cryptographic hashing, HMAC, and secure random bytes inside DuckDB. Adds BLAKE3, BLAKE2b, SHA-3, the SHA-2 family, MD5, and SHA-1 alongside an order-deterministic aggregate hash for dataset checksums and tamper-evident audit trails.",
  "githubUrl": "https://github.com/Query-farm/crypto",
  "cta": {
    "title": "Cryptographic Hashing in SQL",
    "description": "Install the Crypto extension to compute hashes, HMACs, and secure random bytes against any DuckDB column — including aggregate hashes for dataset checksums."
  },
  "category": "transformation",
  "status": "stable",
  "license": "MIT",
  "writtenIn": "C++",
  "sourceAvailable": "https://github.com/Query-farm/crypto",
  "installSource": "community",
  "quickStart": "-- SHA-256 of a string (BLOB output, hex it for display)\nSELECT lower(to_hex(crypto_hash('sha2-256', 'hello world'))) AS sha256;\n\n-- HMAC-SHA256\nSELECT lower(to_hex(crypto_hmac('sha2-256', 'my-secret', 'message'))) AS hmac;\n\n-- 32 cryptographically secure random bytes (e.g. an AES-256 key)\nSELECT crypto_random_bytes(32) AS key;\n\n-- Deterministic checksum of an ordered dataset\nSELECT lower(to_hex(crypto_hash_agg('sha2-256', email ORDER BY email))) AS dataset_hash\nFROM users;",
  "relatedExtensions": ["hashfuncs"]
}
