[
  {
    "name": "sql_auto_complete",
    "categories": ["Autocomplete"],
    "description": "Return ranked autocomplete suggestions for a partial SQL string using the VS-Code-derived fuzzy matcher. Loaded by the DuckDB CLI to power Tab-completion; you can also call it directly in SQL to inspect what the completer would suggest. Replaces the function of the same name from the built-in [autocomplete](https://duckdb.org/docs/extensions/autocomplete.html) extension when this extension is loaded.",
    "parameters": [
      {
        "name": "query",
        "type": "VARCHAR",
        "paramType": "positional",
        "description": "The partial SQL text the user has typed. Suggestions are ranked by fuzzy match score against keywords and identifiers (databases, schemas, tables, columns) currently visible to the session."
      }
    ],
    "examples": [
      {
        "description": "Complete a SQL keyword — the top suggestion for `SEL` is `SELECT`.",
        "code": "SELECT trim(suggestion) AS suggestion\nFROM sql_auto_complete('SEL')\nLIMIT 3;"
      },
      {
        "description": "Suggest a table name after `FROM` — the completer walks every attached database and schema in the search path.",
        "code": "SELECT suggestion\nFROM sql_auto_complete('SELECT * FROM tables')\nLIMIT 1;"
      },
      {
        "description": "Substring + camelCase matching — typing `gnst` finds `getNotificationStatus` even though it isn't a prefix.",
        "code": "SELECT suggestion\nFROM sql_auto_complete('SELECT * FROM gnst');"
      }
    ],
    "relatedFunctions": []
  }
]
