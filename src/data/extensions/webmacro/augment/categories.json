{
  "Loading": "Fetch a SQL macro definition from an HTTP(S) URL and register it in the current DuckDB session. The whole extension is one function — point it at a URL that returns `CREATE MACRO ...` SQL."
}
