{
  "Requests": "Issue HTTP requests from DuckDB SQL. [`http_get`](#http_get) for reads, [`http_post`](#http_post) for JSON-body POSTs, [`http_post_form`](#http_post_form) for `application/x-www-form-urlencoded` POSTs. Each returns a JSON object with `status`, `reason`, and `body`."
}
