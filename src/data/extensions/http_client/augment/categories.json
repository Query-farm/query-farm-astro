{
  "Requests": "Synchronous HTTP requests from SQL. GET for reads, POST (JSON) for REST APIs that accept JSON, POST (form-encoded) for legacy APIs and HTML forms. All return the same `{status, reason, body, headers}` struct."
}
