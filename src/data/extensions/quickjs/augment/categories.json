{
  "Execute": "Run JavaScript code from SQL — `quickjs(...)` for general expressions and array → table fan-out, `quickjs_eval(fn, ...args)` for invoking a JS function with positional arguments and getting JSON back."
}
