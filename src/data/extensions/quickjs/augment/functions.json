[
  {
    "name": "quickjs",
    "categories": ["Execute"],
    "description": "Evaluate JavaScript code and return the result. Has both a scalar form (returns a single value as VARCHAR) and a table-valued form (when the JS returns an array, each element becomes a row).",
    "examples": [
      { "description": "Simple expression", "code": "SELECT quickjs('2 + 2');" },
      { "description": "Array → table", "code": "SELECT * FROM quickjs('[1, 2, 3, 4, 5]');" },
      { "description": "Array with parameter passthrough", "code": "SELECT * FROM quickjs('parsed_arg0.map(x => x * arg1)', '[1, 2, 3, 4, 5]', 3);" }
    ],
    "relatedFunctions": ["quickjs_eval"]
  },
  {
    "name": "quickjs_eval",
    "categories": ["Execute"],
    "description": "Call a JavaScript function with the given arguments. The first argument is a JS function expression; subsequent arguments are passed positionally. The result is serialized as JSON.",
    "examples": [
      { "description": "Two-argument adder", "code": "SELECT quickjs_eval('(a, b) => a + b', 5, 3);" },
      { "description": "Custom JSON munging — better than chaining json_extract_*", "code": "SELECT quickjs_eval('(payload) => { const d = JSON.parse(payload); return { user: d.user, os: d.meta.os }; }', payload) AS extracted FROM events;" }
    ],
    "relatedFunctions": ["quickjs"]
  }
]
