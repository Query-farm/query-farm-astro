{
  "name": "tributary",
  "displayName": "Tributary",
  "icon": "🌊",
  "description": "Read Apache Kafka topics from DuckDB SQL. Scan a topic into a query, snapshot it into a table, JOIN against local data, or inspect cluster metadata — all without a separate Kafka client. Authentication, partitions, and offsets are configured through the underlying librdkafka named-parameter surface.",
  "githubUrl": "https://github.com/Query-farm/tributary",
  "docsUrl": "https://query.farm/duckdb_extension_tributary.html",
  "cta": {
    "title": "Kafka topics as DuckDB tables",
    "description": "Install Tributary to point SELECT queries directly at Kafka topics — snapshot, join, and analyze without writing a consumer."
  },
  "category": "connectors",
  "status": "experimental",
  "license": "MIT",
  "writtenIn": "C++",
  "sourceAvailable": "https://github.com/Query-farm/tributary",
  "installSource": "community",
  "quickStart": "-- Scan an entire Kafka topic\nSELECT *\nFROM tributary_scan_topic('test-topic',\n                          \"bootstrap.servers\" := 'localhost:9092');",
  "notice": {
    "type": "warning",
    "title": "Successor extension expected soon",
    "body": "A more feature-complete Kafka extension is in active development and is expected to supersede this one. The successor will add **producer support** (write-back to topics), **Schema Registry integration** (Avro / Protobuf / JSON Schema lookups), and **transactional semantics** (exactly-once consume + produce). If you're starting a new project against Kafka from DuckDB, plan for a migration when that lands. The current Tributary extension remains the right tool today for read-side analytics, snapshots, and cluster inspection."
  }
}
