{
  "name": "redis",
  "displayName": "Redis",
  "icon": "database",
  "description": "Read and write Redis from DuckDB SQL. Strings, hashes, and lists; cursor-paginated SCAN/HSCAN; and a single-query bulk-dump pattern that drains an entire Redis keyspace into a DuckDB table. Best fit: operational queries, hot-cold data joins, and one-off migrations — not application-grade Redis access.",
  "githubUrl": "https://github.com/Query-farm/redis",
  "cta": {
    "title": "SQL access to your Redis instance",
    "description": "Install Redis to read/write strings, hashes, and lists from inside DuckDB queries — and to drain an entire keyspace into a table with one CREATE TABLE AS."
  },
  "category": "connectors",
  "status": "experimental",
  "license": "MIT",
  "writtenIn": "C++",
  "sourceAvailable": "https://github.com/Query-farm/redis",
  "installSource": "community",
  "quickStart": [
    {
      "title": "Connect via a secret",
      "sql": "CREATE SECRET IF NOT EXISTS redis (\n  TYPE redis,\n  PROVIDER config,\n  host 'localhost',\n  port '6379'\n);"
    },
    {
      "title": "Read and write strings",
      "sql": "CREATE SECRET IF NOT EXISTS redis (\n  TYPE redis,\n  PROVIDER config,\n  host 'localhost',\n  port '6379'\n);\n\nSELECT redis_set('user:1', 'John Doe', 'redis') AS ok;\nSELECT redis_get('user:1', 'redis') AS name;"
    },
    {
      "title": "Discover keys matching a pattern",
      "sql": "CREATE SECRET IF NOT EXISTS redis (\n  TYPE redis,\n  PROVIDER config,\n  host 'localhost',\n  port '6379'\n);\n\nSELECT * FROM redis_keys('user:*', 'redis');"
    }
  ]
}
