[
  {
    "name": "redis",
    "category": "Cache / In-memory store",
    "description": "Redis connection details. All redis_* functions take a final `secret` argument naming a secret of this type.",
    "parameters": [
      { "name": "host", "type": "VARCHAR", "required": true, "description": "Redis server hostname or IP." },
      { "name": "port", "type": "VARCHAR", "required": true, "description": "Redis server port (typically '6379')." },
      { "name": "password", "type": "VARCHAR", "required": false, "description": "Authentication password if the server requires AUTH." }
    ],
    "examples": [
      {
        "description": "Local Redis without auth",
        "code": "CREATE SECRET IF NOT EXISTS redis (\n  TYPE redis,\n  PROVIDER config,\n  host 'localhost',\n  port '6379'\n);"
      },
      {
        "description": "Redis Cloud with TLS endpoint",
        "code": "CREATE SECRET IF NOT EXISTS redis (\n  TYPE redis,\n  PROVIDER config,\n  host 'redis-1234.ec2.redns.redis-cloud.com',\n  port '16959',\n  password 'xxxxxx'\n);"
      }
    ]
  }
]
