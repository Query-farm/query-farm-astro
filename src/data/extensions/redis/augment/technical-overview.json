{
  "eyebrow": "Technical Overview",
  "title": "Reach the cache without the app-layer glue",
  "description": "The usual path from DuckDB to [Redis](https://redis.io/docs/latest/) runs through app-layer Python or Node.js glue. This extension removes it: read and write a live instance from SQL. Best for operational queries, hot-cold data joins, and one-off migrations; for application-grade Redis access, keep a real client.",
  "sections": [
    {
      "icon": "📕",
      "title": "What it is",
      "description": "Redis is the most common in-memory cache and quick-lookup store, and it usually sits one network hop away from data you already keep in DuckDB. The normal way to bridge the two is to write a script — pull keys out of Redis in Python or Node, reshape them, load them into a table. This extension removes that middle layer: it exposes Redis operations as `redis_*` SQL functions so the cache becomes just another source you can `SELECT` from, `JOIN` against, and write back to. It is scoped deliberately to the SQL-side workflows where data integration matters — draining a keyspace into a table, joining cached lookups against bulk data, priming a cache from analytical output, auditing keys — rather than to being a complete Redis client."
    },
    {
      "icon": "🔌",
      "title": "How it works",
      "description": "Connection details live in a [DuckDB secret](https://duckdb.org/docs/configuration/secrets_manager) of `TYPE redis`, and every `redis_*` function takes a trailing argument naming that secret. This keeps credentials out of query text and lets dev / staging / prod live as separate secrets you select per query. There is no `ATTACH` interface and no virtual catalog — this is purely a function-based extension, so Redis access composes inline anywhere a scalar function or table function is allowed.",
      "bulletPoints": [
        {
          "label": "Secret-based configuration",
          "description": "A `redis` secret holds `host`, `port`, and an optional `password`. See [`CREATE SECRET`](https://duckdb.org/docs/sql/statements/create_secret); parameterize the password from the environment so it never lands in a cached query plan."
        },
        {
          "label": "RESP over TCP",
          "description": "The extension speaks the native [Redis Serialization Protocol](https://redis.io/docs/latest/develop/reference/protocol-spec/) directly over a plain TCP socket — no separate Redis daemon, sidecar, or driver process is involved."
        },
        {
          "label": "Connection reuse",
          "description": "Connections to a given secret are pooled and reused across calls rather than reopened per row, so per-row lookups inside a `SELECT` and streaming table functions amortize the connection cost."
        }
      ]
    },
    {
      "icon": "🛡️",
      "title": "Production caveats",
      "description": "Be aware before pointing this at a busy Redis instance. The extension is honest-by-design about what it doesn't yet do.",
      "bulletPoints": [
        {
          "label": "Use SCAN, not KEYS",
          "description": "[`KEYS`](https://redis.io/docs/latest/commands/keys/) blocks the Redis server for the duration of the scan — fine in dev, dangerous on production keyspaces. Prefer the cursor-paginated [`SCAN`](https://redis.io/docs/latest/commands/scan/)-backed functions, which iterate without stalling traffic."
        },
        {
          "label": "Strings, hashes, and lists only",
          "description": "No SET / ZSET / STREAM / PUB-SUB / TRANSACTIONS / SCRIPTING today. See [Redis data types](https://redis.io/docs/latest/develop/data-types/) for what's supported elsewhere."
        },
        {
          "label": "Single-node connections",
          "description": "No [Redis Cluster](https://redis.io/docs/latest/operate/oss_and_stack/management/scaling/) topology awareness. Point the secret at a single endpoint."
        },
        {
          "label": "Plaintext auth",
          "description": "The `password` field in the secret is sent over plain TCP — TLS is not yet wired up. Use the extension on trusted networks until that lands. See [Redis security](https://redis.io/docs/latest/operate/oss_and_stack/management/security/) for the upstream guidance."
        },
        {
          "label": "Experimental status",
          "description": "The function surface may change as more Redis primitives land. Pin a known-good extension version in production CTAS jobs."
        }
      ]
    }
  ]
}
