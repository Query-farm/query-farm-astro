{
  "Scheduling": "Register, list, and cancel cron-style scheduled SQL queries inside a DuckDB process. Six-field cron expressions with second-level resolution. Jobs run only while the DuckDB process is alive — restart-safe scheduling needs an external orchestrator."
}
