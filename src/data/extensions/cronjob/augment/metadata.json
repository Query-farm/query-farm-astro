{
  "name": "cronjob",
  "displayName": "Cronjob",
  "icon": "⏰",
  "description": "Schedule SQL queries to run on a cron expression — but only while the DuckDB process is alive. Six-field expressions with second-level resolution, list/inspect/cancel from SQL. Best fit: periodic refreshes, recurring exports, scheduled webhook alerts, and maintenance tasks inside a long-running DuckDB process.",
  "githubUrl": "https://github.com/Query-farm/cronjob",
  "cta": {
    "title": "Cron-style scheduling inside DuckDB",
    "description": "Install Cronjob to register repeating SQL tasks — refresh aggregates, export reports, run ANALYZE, fire webhooks — that tick along while your DuckDB process is running."
  },
  "category": "devtools",
  "status": "experimental",
  "license": "MIT",
  "writtenIn": "C++",
  "sourceAvailable": "https://github.com/Query-farm/cronjob",
  "installSource": "community",
  "quickStart": "-- Run a query every 15 seconds during hours 1–4\nSELECT cron('SELECT now()', '*/15 * 1-4 * * *');\n\n-- Inspect scheduled jobs\nSELECT * FROM cron_jobs();\n\n-- Cancel a job by its job_id\nSELECT cron_delete('task_0');",
  "relatedExtensions": ["events"]
}
