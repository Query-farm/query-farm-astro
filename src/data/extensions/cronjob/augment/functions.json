[
  {
    "name": "cron",
    "categories": [
      "Scheduling"
    ],
    "description": "Register a SQL query to run on a six-field cron expression (`second minute hour day-of-month month day-of-week`). Returns a `job_id` (e.g. `task_0`) you can later pass to `cron_delete` or look up in `cron_jobs`. The schedule fires only while this DuckDB process is alive — when the process exits, the registration is gone. Use crontab.guru to sanity-check expressions before registering.",
    "examples": [
      {
        "description": "Every 15 seconds during hours 1–4",
        "code": "SELECT cron('SELECT now()', '*/15 * 1-4 * * *');"
      },
      {
        "description": "Weekdays at 7am — daily metrics export",
        "code": "SELECT cron('CALL daily_metrics_export()', '0 0 7 ? * MON-FRI');"
      },
      {
        "description": "Top of every hour — refresh a materialized table",
        "code": "SELECT cron('CREATE OR REPLACE TABLE hourly_rollup AS SELECT ...', '0 0 * * * *');"
      }
    ],
    "relatedFunctions": [
      "cron_jobs",
      "cron_delete"
    ]
  },
  {
    "name": "cron_jobs",
    "categories": [
      "Scheduling"
    ],
    "description": "Table function listing every scheduled job in this process — `job_id`, `query`, `schedule`, `next_run`, `status`, `last_run`, and `last_result`. The introspection primitive: filter by status to find failing jobs, or join `last_result` against expected output to assert that recent runs succeeded.",
    "examples": [
      {
        "description": "List all scheduled jobs",
        "code": "SELECT * FROM cron_jobs();"
      },
      {
        "description": "Find jobs that have never run",
        "code": "SELECT job_id, schedule FROM cron_jobs() WHERE last_run IS NULL;"
      }
    ],
    "relatedFunctions": [
      "cron",
      "cron_delete"
    ]
  },
  {
    "name": "cron_delete",
    "categories": [
      "Scheduling"
    ],
    "description": "Cancel a scheduled job by `job_id` (the value returned from `cron` or visible in `cron_jobs`). Returns `TRUE` if a matching job was found and removed. Process restarts also cancel jobs — `cron_delete` is for un-registering while DuckDB is still running.",
    "examples": [
      {
        "description": "Cancel a specific job",
        "code": "SELECT cron_delete('task_0');"
      }
    ],
    "relatedFunctions": [
      "cron",
      "cron_jobs"
    ]
  }
]
