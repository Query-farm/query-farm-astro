{
  "eyebrow": "Technical Overview",
  "title": "A scheduler that lives inside the process",
  "description": "A recurring query normally means an external scheduler and a shell wrapper around the CLI. Cronjob moves the schedule inside the database instead: register SQL against a [cron expression](https://en.wikipedia.org/wiki/Cron) and DuckDB runs it itself. The catch is the scope: schedules live in memory inside the running DuckDB process. When the process exits, every job is forgotten. Best fit when DuckDB is already a long-running service (a server, a daemon, an analytics worker), not a CLI you start and stop.",
  "sections": [
    {
      "icon": "⚙️",
      "title": "How it works",
      "description": "Cronjob turns a long-running DuckDB process into its own task scheduler. Registering a query records an entry in an in-memory job table; a single background thread inside the process wakes on each tick, finds due jobs, and runs their SQL through the connection that registered them. There is no separate process, no daemon, and nothing on disk — the scheduler *is* the DuckDB process.",
      "bulletPoints": [
        {
          "label": "A background thread on a tick loop",
          "description": "One thread owned by the extension wakes periodically, evaluates which registered jobs are due, executes each due query, and writes the outcome (status, last run time, last result or error message) back into the job table. The introspection table surfaces all of that so you can monitor schedules from SQL like any other relation."
        },
        {
          "label": "Six-field cron expressions",
          "description": "Schedules use the six-field form `second minute hour day-of-month month day-of-week` — one more field than POSIX, giving second-level resolution. The standard `*`, `?`, `,`, `-`, `/` operators and `MON`–`SUN` weekday names all work. [crontab.guru](https://crontab.guru/) is handy for sanity-checking, but note it parses the five-field POSIX form, so the leading `seconds` field here is an extension."
        },
        {
          "label": "Same connection, same access",
          "description": "A scheduled query runs against the database the scheduler was registered from, inheriting that session's access rights, attached databases, and secrets. Anything you can run interactively, you can schedule — including `CREATE OR REPLACE TABLE`, `COPY ... TO`, `ANALYZE`, or a webhook-firing query. One caveat: register against an in-memory database and the schedule disappears when that database does, even before process exit."
        },
        {
          "label": "Sequential per job",
          "description": "Each registered job is single-threaded against itself: if a tick's query runs longer than the schedule interval, the next tick for that job waits its turn. There is no overlap and no parallel re-entry of the same registration."
        }
      ]
    },
    {
      "icon": "🛡️",
      "title": "Production caveats",
      "description": "The scheduler's strength — living inside the process — is also the source of every limitation. Internalize these before scheduling anything important.",
      "bulletPoints": [
        {
          "label": "Schedules die with the process",
          "description": "This is the central caveat. A `kill -9`, a deploy, an OOM, or a clean shutdown all wipe the in-memory registry — there is no WAL entry, no on-disk table, and no resume. If the work must happen on a wall-clock cadence regardless of process state, drive the run from an external orchestrator (system [`cron(8)`](https://man7.org/linux/man-pages/man8/cron.8.html), [`systemd` timers](https://www.freedesktop.org/software/systemd/man/systemd.timer.html), [Kubernetes CronJob](https://kubernetes.io/docs/concepts/workloads/controllers/cron-jobs/)) and have *that* invoke DuckDB."
        },
        {
          "label": "No multi-process coordination",
          "description": "Two DuckDB processes that both load Cronjob and register the same job will both run it. There is no leader election and no lease — pick one process to own the schedule, or de-duplicate downstream."
        },
        {
          "label": "Errors are recorded, not retried",
          "description": "A failed query writes its error message into the job's last-result column. The extension does not retry, back off, or alert — wrap the scheduled query in its own error-reporting logic (write to an audit table, fire a webhook) if you need notification."
        },
        {
          "label": "Long-running queries block the next tick",
          "description": "Because each job is sequential against itself, a query that overruns its interval delays its own next run. Schedule with headroom, or split heavy work so no single run can starve the cadence."
        },
        {
          "label": "Experimental status",
          "description": "Marked experimental upstream. The function surface is small and stable in spirit, but pin a known-good extension version in production until it's promoted."
        }
      ]
    }
  ]
}
