{
  "name": "bitfilters",
  "displayName": "Bitfilters",
  "icon": "🎯",
  "description": "Probabilistic set-membership filters as DuckDB columns. Quotient, XOR (8/16-bit), Binary Fuse (8/16-bit), and DuckDB-style Bloom filters — answer 'is X in this set?' in O(1) using a fraction of the memory of the source set, with tunable false-positive rates and zero false negatives.",
  "githubUrl": "https://github.com/Query-farm/bitfilters",
  "cta": {
    "title": "Skip Expensive Operations Probabilistically",
    "description": "Install Bitfilters to materialize compact, fast set-membership filters as DuckDB blobs. Pre-filter joins, dodge cache misses, skip data partitions — all with measurable false-positive bounds."
  },
  "category": "performance",
  "status": "stable",
  "license": "MIT",
  "writtenIn": "C++",
  "sourceAvailable": "https://github.com/Query-farm/bitfilters",
  "installSource": "community",
  "quickStart": "-- Build an xor8 filter from hashed keys (1% false-positive rate, ~9 bits per key)\nWITH f AS (\n  SELECT xor8_filter(hash(email)) AS filter FROM users\n)\nSELECT email,\n       xor8_filter_contains(f.filter, hash(email)) AS might_be_known\nFROM new_signups, f\nLIMIT 10;\n\n-- Per-partition Quotient filter — supports deletion and resize\nSELECT bucket,\n       quotient_filter(16, 4, hash(id)) AS filter\nFROM ids\nGROUP BY bucket;",
  "relatedExtensions": ["hashfuncs"]
}
