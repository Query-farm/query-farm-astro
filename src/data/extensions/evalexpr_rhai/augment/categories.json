{
  "Execute": "Run [Rhai](https://rhai.rs/) scripts inside SQL — single-function surface. Pass an optional `JSON` context to expose per-row variables as `context.<field>`. The result is a `UNION(ok JSON, \"error\" VARCHAR)` so script failures don't break the surrounding query — read `.ok` on success, `.error` on failure."
}
