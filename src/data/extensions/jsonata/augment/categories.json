{
  "Query": "Evaluate JSONata expressions against JSON. The single function — `jsonata(expr, json [, bindings])` — covers everything from simple path lookups to deep transformations. JSONata reference: https://jsonata.org"
}
