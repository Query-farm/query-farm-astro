{
  "name": "radio",
  "displayName": "Radio",
  "icon": "broadcast",
  "description": "Two-way real-time messaging from DuckDB SQL. Subscribe to WebSocket and Redis Pub/Sub channels, query received events as ordinary tables, and queue outgoing messages with status tracking. Best fit: in-process pipelines, dashboards, and ad-hoc inspection — buffers are in-memory, scoped to the DuckDB process.",
  "githubUrl": "https://github.com/Query-farm/radio",
  "docsUrl": "https://query.farm/duckdb_extension_radio.html",
  "cta": {
    "title": "Real-time messaging in SQL",
    "description": "Install Radio to subscribe to WebSocket / Redis Pub/Sub channels, drain incoming events as queryable rows, and queue outgoing messages — all from SQL."
  },
  "category": "connectors",
  "status": "experimental",
  "license": "MIT",
  "writtenIn": "C++",
  "sourceAvailable": "https://github.com/Query-farm/radio",
  "installSource": "community",
  "quickStart": [
    {
      "title": "Subscribe to a WebSocket channel — the URL is the subscription key",
      "sql": "CALL radio_subscribe('wss://stream.example.com/events');"
    },
    {
      "title": "Drain buffered messages as a normal table",
      "sql": "SELECT receive_time, channel, message\nFROM radio_subscription_received_messages('wss://stream.example.com/events')\nORDER BY receive_time DESC\nLIMIT 50;"
    },
    {
      "title": "Send a message — args are (url, channel, payload, max_attempts, retry_delay)",
      "sql": "CALL radio_transmit_message(\n  'wss://stream.example.com/events',\n  'main',\n  '{\"type\":\"hello\"}'::BLOB,\n  3,\n  INTERVAL '500 milliseconds'\n);"
    }
  ]
}
