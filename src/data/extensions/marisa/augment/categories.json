{
  "Build": "The aggregate that turns a column of strings into a single MARISA trie BLOB. Build once at ingest time, store the BLOB, query repeatedly.",
  "Query": "Membership tests, prefix searches, and predictive completions against an existing trie. All run in O(|key|) — the lookup work doesn't scale with the size of the dictionary."
}
