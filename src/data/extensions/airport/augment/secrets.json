[
  {
    "name": "airport",
    "category": "Arrow Flight Connection",
    "description": "Connection details for an Arrow Flight server. Used by `ATTACH` to establish a database connection and by the discovery / operations functions when invoked with a secret reference. `scope` is the Flight URL the secret applies to (DuckDB Secrets Manager scope-matches against it).",
    "parameters": [
      { "name": "scope",      "type": "VARCHAR", "required": true,  "description": "Arrow Flight server URI, e.g. `grpc://host:8815` or `grpc+tls://host:8815`. The DuckDB Secrets Manager matches `ATTACH` / function calls to a secret by URL prefix against this value." },
      { "name": "auth_token", "type": "VARCHAR", "required": false, "description": "Bearer token sent in the gRPC Authorization header." }
    ],
    "examples": [
      {
        "description": "Create a secret for a TLS-protected Flight server",
        "code": "CREATE SECRET my_flight (\n  TYPE airport,\n  scope 'grpc+tls://flight.example.com:8815',\n  auth_token 'your-token'\n);"
      },
      {
        "description": "Attach the database — the matching secret is picked up automatically by URL scope",
        "code": "ATTACH 'grpc+tls://flight.example.com:8815' AS remote (TYPE airport);\nSELECT * FROM remote.public.events LIMIT 10;"
      }
    ]
  }
]
