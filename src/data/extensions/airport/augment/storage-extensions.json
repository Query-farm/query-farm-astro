[
  {
    "id": "airport",
    "name": "airport",
    "category": "Arrow Flight Catalog",
    "description": "Attach an Arrow Flight server as a DuckDB catalog. Once attached, schemas, tables, and SQL operations on the remote server become accessible like any local database — joins, filters, aggregates, and (when the server supports the corresponding Flight RPCs) `INSERT` / `UPDATE` / `DELETE`. Pass the Flight URL as the database identifier; the matching `airport` secret (if any) is picked up automatically by URL scope. The optional `location` ATTACH parameter overrides the URL when you want a logical name as the database identifier.",
    "parameters": [
      {
        "name": "location",
        "type": "VARCHAR",
        "required": false,
        "description": "Optional Arrow Flight server URI override (e.g. `grpc://host:8815` or `grpc+tls://host:8815`). When omitted, the URL passed as the database identifier in `ATTACH '<url>' AS <alias>` is used."
      },
      {
        "name": "auth_token",
        "type": "VARCHAR",
        "required": false,
        "description": "Bearer token sent in the gRPC Authorization header. Recommended via [`CREATE SECRET`](#secrets) rather than inline so it's not stored in query text."
      }
    ],
    "examples": [
      {
        "description": "Attach by URL with credentials picked up from a matching secret (recommended)",
        "code": "-- 1. Stash credentials once. `scope` is the URL prefix the secret applies to.\nCREATE SECRET my_flight (\n  TYPE airport,\n  scope 'grpc+tls://flight.example.com:8815',\n  auth_token :token\n);\n\n-- 2. Attach the URL — the Secrets Manager picks up `my_flight`\n--    automatically because the URL matches its scope.\nATTACH 'grpc+tls://flight.example.com:8815' AS remote (TYPE airport);\n\n-- 3. Query as if it were local\nSELECT user_id, COUNT(*) AS events\nFROM remote.public.activity\nWHERE ts >= CURRENT_DATE - 7\nGROUP BY user_id;"
      },
      {
        "description": "Attach inline without a secret (good for ad-hoc connections to unauthenticated servers)",
        "code": "ATTACH 'grpc://localhost:8815' AS remote (TYPE airport);\n\nSHOW TABLES FROM remote.public;"
      },
      {
        "description": "Mutate a remote table — works only when the Flight server implements `DoPut` for the operation",
        "code": "ATTACH 'grpc+tls://flight.example.com:8815' AS remote (TYPE airport);\n\nINSERT INTO remote.public.events\nSELECT user_id, event_type, ts\nFROM staging.new_events;\n\nUPDATE remote.public.profile\n  SET country = 'CA'\n  WHERE user_id = 12345;"
      }
    ]
  }
]
