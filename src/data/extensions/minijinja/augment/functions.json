[
  {
    "name": "minijinja_render",
    "categories": ["Render"],
    "description": "Render a MiniJinja (Jinja2-compatible) template with a JSON context. Supports the full Jinja vocabulary: variable expressions, filters, conditionals, for/while loops, and template-level autoescaping. Optional named arguments tune behavior (e.g. `autoescape := false`).",
    "examples": [
      { "description": "Basic substitution", "code": "SELECT minijinja_render('{{ foo }}', '{\"foo\":\"bar\"}');" },
      { "description": "Nested fields", "code": "SELECT minijinja_render('Hello {{ user.name }}, {{ user.messages }} new', '{\"user\":{\"name\":\"Alice\",\"messages\":5}}');" },
      { "description": "For loop with comma separator", "code": "SELECT minijinja_render('{% for x in xs %}{{ x }}{% if not loop.last %}, {% endif %}{% endfor %}', '{\"xs\":[\"A\",\"B\",\"C\"]}');" },
      { "description": "Disable autoescape for raw text", "code": "SELECT minijinja_render('{{ v }}', '{\"v\":\"B&O\"}', autoescape := false);" }
    ],
    "relatedFunctions": []
  }
]
