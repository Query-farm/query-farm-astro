{
  "eyebrow": "Technical Overview",
  "title": "Why Use Hashfuncs?",
  "description": "Industry-standard, non-cryptographic hashes inside DuckDB SQL — [xxHash](https://cyan4973.github.io/xxHash/), [MurmurHash3](https://github.com/aappleby/smhasher), and [RapidHash](https://github.com/Nicoshev/rapidhash). These are speed-first hashes: fast and well-distributed against random input, where the only \"adversary\" is randomness, not a person who controls the bytes. For digests, signatures, or HMAC, use the [crypto](/products/extensions/crypto) extension instead.",
  "sections": [
    {
      "icon": "⚡",
      "title": "What it is",
      "description": "Hashfuncs gives you fast, deterministic 32-, 64-, and 128-bit hashes — the slot between DuckDB's built-in [`hash()`](https://duckdb.org/docs/sql/functions/utility) (great for join/group-by internals, but implementation-defined and unsafe to persist or reproduce elsewhere) and a cryptographic hash (an order of magnitude slower, and unnecessary when nothing adversarial is at stake). The natural workloads are partitioning, sharding, deduplication, cache keys, and probabilistic-filter inputs — anywhere you want a stable, well-distributed value derived from data at near-RAM bandwidth."
    },
    {
      "icon": "🧭",
      "title": "Hashfuncs vs. crypto — picking the right extension",
      "description": "These two extensions look similar but solve different problems. Pick by the threat model, not by output width.",
      "bulletPoints": [
        {
          "label": "Use hashfuncs when speed wins",
          "description": "Partitioning, sharding, deduplication, cache keys, [Bloom-filter](https://en.wikipedia.org/wiki/Bloom_filter) inputs — workloads where the adversary is randomness, not a human. `xxh3_64` and `rapidhash` run roughly an order of magnitude faster than SHA-256."
        },
        {
          "label": "Use crypto when collisions must be hard to forge",
          "description": "Digital signatures, HMAC, content-addressed storage with adversarial input, tamper-evident audit logs, password salts, secure random bytes — anything an attacker could try to break. The [crypto](/products/extensions/crypto) extension gives you BLAKE3, SHA-2, SHA-3, HMAC, and a CSPRNG."
        },
        {
          "label": "Don't substitute one for the other",
          "description": "These hashes are explicitly **not** collision-resistant against a motivated adversary. Treating an `xxh3_128` like a SHA-256 digest is a security bug, not a performance optimization. The inverse is also wrong: spending SHA-256 on partition routing is wasted CPU. (For password storage, neither family is the answer — use a real password hash like bcrypt or argon2 outside DuckDB.)"
        },
        {
          "label": "They compose",
          "description": "Both extensions can be loaded together. A common pattern: [`crypto`](/products/extensions/crypto) for the durable identity hash on a row, `hashfuncs` for the routing hash that decides which shard processes it."
        }
      ]
    },
    {
      "icon": "🔧",
      "title": "How it works",
      "description": "Every function is a scalar SQL function with one or two arguments — the value, and an optional seed. Each family is an upstream-faithful C/C++ implementation linked into the extension binary: no catalog, no secrets, no network, every call pure CPU.",
      "bulletPoints": [
        {
          "label": "Three families, one interface",
          "description": "[xxHash](https://github.com/Cyan4973/xxHash) (32 / 64 / 128-bit, including a hex digest; XXH3 variants are tuned for SSE2 / AVX2 / NEON), [MurmurHash3](https://github.com/aappleby/smhasher) (32-bit plus x86 and x64 128-bit forms), and [RapidHash](https://github.com/Nicoshev/rapidhash), a newer 64-bit hash that often wins published throughput benchmarks. Every function accepts any DuckDB scalar value as input."
        },
        {
          "label": "Optional seeds for independent hash spaces",
          "description": "Every function has a seeded overload. Two different seeds give two *uncorrelated* hash spaces from the same column — exactly what you need for double-hashing in [Bloom filters](https://en.wikipedia.org/wiki/Bloom_filter) or running two parallel shardings whose bucket assignments must not correlate."
        },
        {
          "label": "Canonical hex digest for cross-language reproducibility",
          "description": "The 128-bit xxHash variant can emit the lowercase 32-character canonical hex form defined in the [xxHash specification](https://github.com/Cyan4973/xxHash/blob/dev/doc/xxhash_spec.md), in `low64 || high64` byte order. That digest matches what [Python xxhash](https://pypi.org/project/xxhash/), [xxhash-rust](https://docs.rs/xxhash-rust/latest/xxhash_rust/), and `XXH128_canonicalFromHash` in C produce over the same bytes — so a fingerprint round-trips across SQL and application code."
        },
        {
          "label": "Feeds the bitfilters extension directly",
          "description": "The 64-bit `UBIGINT`-returning hashes (`xxh3_64`, `rapidhash`) are the natural input to the [bitfilters](/products/extensions/bitfilters) extension's Bloom, XOR, Quotient, and Binary Fuse filters — sub-microsecond probabilistic set-membership built entirely in one DuckDB session."
        }
      ]
    }
  ]
}
