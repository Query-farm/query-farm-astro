{
  "Validate": "Verify that a JSON Schema is well-formed and that data conforms to it. Both checks return BOOLEAN — easy to drop into WHERE clauses for filtering or quality dashboards.",
  "Enrich": "Compute JSON-Patch operations that fill in defaults declared in the schema. Apply the patch to bring incomplete JSON documents into conformance without rewriting them by hand."
}
