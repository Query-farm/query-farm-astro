{
  "name": "json_schema",
  "displayName": "JSON Schema",
  "icon": "📐",
  "description": "JSON Schema validation and default-value enrichment in SQL. Verify that JSON columns conform to a schema, validate the schema itself, and generate JSON-Patch operations that fill in missing defaults.",
  "githubUrl": "https://github.com/Query-farm/json_schema",
  "cta": {
    "title": "Validate JSON in SQL",
    "description": "Install JSON Schema to enforce data contracts on JSON columns at query time — perfect for API ingest, ETL data-quality gates, and config validation."
  },
  "category": "quality",
  "status": "stable",
  "license": "MIT",
  "writtenIn": "Rust",
  "sourceAvailable": "https://github.com/Query-farm/json_schema",
  "installSource": "community",
  "quickStart": "-- Validate a JSON document against a schema\nSELECT json_schema_validate(:schema, payload) AS ok\nFROM events;\n\n-- Verify the schema itself is well-formed\nSELECT json_schema_validate_schema(:schema) AS schema_ok;\n\n-- Compute a JSON-Patch that fills in missing defaults\nSELECT json_schema_patch(:schema, payload) AS patch FROM events;",
  "relatedExtensions": ["jsonata"]
}
