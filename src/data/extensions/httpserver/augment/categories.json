{
  "Server": "Start and stop the embedded HTTP query listener. The third argument to `httpserve_start` selects the auth mode — empty (no auth), `user:pass` (HTTP Basic), or any other value (shared key in `X-API-Key`)."
}
