{
  "Server": "Start and stop the embedded HTTP API server. Configuration is positional — host, port, and an auth string that distinguishes empty (no auth) / `user:pass` (Basic-auth) / anything else (X-API-Key)."
}
