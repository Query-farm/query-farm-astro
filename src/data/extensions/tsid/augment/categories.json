{
  "Generation": "Mint a new TSID. Calling `tsid()` repeatedly produces strictly increasing values (by string compare) suitable as primary keys.",
  "Inspection": "Extract the timestamp embedded in a TSID. Lets you recover insertion time without storing a separate timestamp column."
}
