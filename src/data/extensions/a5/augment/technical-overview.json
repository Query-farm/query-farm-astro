{
  "eyebrow": "Technical Overview",
  "title": "Every cell covers the same area on the globe",
  "description": "A5 is a [pentagonal discrete global grid system](https://a5geo.org/docs/technical/dggs), in the same family as Uber's [H3](https://h3geo.org/) (hexagonal) and Google's [S2](https://s2geometry.io/) (square). It turns a `(lon, lat)` point into a single 64-bit integer cell ID that you can `GROUP BY`, `JOIN`, and index, the same way you would an H3 or S2 ID. What makes A5 different is that every cell at a given resolution covers the same area on the globe.",
  "sections": [
    {
      "icon": "⬟",
      "title": "Why pentagons, and why equal area",
      "description": "A discrete global grid system chops the sphere into cells so you can drop points into buckets and count them. Those counts only mean something if the cells are about the same size wherever you look. A5 holds cell area uniform across the whole globe, and pentagons are what it uses to get there.",
      "image": "/images/a5-cells.svg",
      "imageAlt": "Real A5 cells tiling a globe — pentagons of equal area across the visible hemisphere",
      "imageCaption": "Real A5 cells (resolution 2) on the globe. From a dodecahedral subdivision: pentagons everywhere, no special-case cells, and equal area at every resolution.",
      "bulletPoints": [
        {
          "label": "The problem A5 solves",
          "description": "When you bucket `(lon, lat)` points into grid cells and compare counts, the comparison is only honest if the cells are the same size. Grids built on lat/lon rectangles or projected squares stretch badly toward the poles, so a count of 100 near the equator and 100 near a pole describe very different densities. A5 cells are equal-area at each resolution by construction, so cross-latitude density comparisons need no per-cell normalization."
        },
        {
          "label": "Pentagons are the cost of that uniformity",
          "description": "You cannot tile a sphere with regular hexagons alone — H3 pays for its hexagons with [12 unavoidable pentagon exceptions](https://h3geo.org/docs/core-library/coordsystems) at the icosahedral vertices that traversal code must special-case. A5 instead derives its cells from a [dodecahedral subdivision](https://a5geo.org/examples/teohedron-dodecahedron), giving pentagons *everywhere* — uniform shape, uniform area, and no exceptional cells to handle. The geometric argument is laid out on the [A5 motivation page](https://a5geo.org/docs/motivation)."
        },
        {
          "label": "It's a thin binding over the reference library",
          "description": "The DuckDB functions wrap the upstream [A5 Rust library](https://github.com/felixpalmer/a5), so cell IDs are identical to what the reference implementation produces — a DuckDB-computed ID round-trips through the JS/TypeScript library and back unchanged."
        }
      ]
    },
    {
      "icon": "🧭",
      "title": "How a point becomes a cell ID",
      "description": "Encoding a coordinate is a deterministic geometric pipeline, and the resulting 64-bit unsigned integer (`UBIGINT`) is self-describing — it carries its own resolution, which is why you never need a separate level column alongside it.",
      "bulletPoints": [
        {
          "label": "From sphere to cell",
          "description": "A point is located on the globe, mapped onto the face of the dodecahedron it falls in, and then placed within that face's recursive pentagonal subdivision down to the requested resolution. Resolution 0 is the small set of base cells covering the whole globe; each finer level subdivides every cell, so cell count grows by a fixed branching factor per level and resolution 30 reaches sub-square-meter precision."
        },
        {
          "label": "Resolution is packed into the ID",
          "description": "The cell's position in the subdivision hierarchy *and* its resolution level are encoded together into the single `UBIGINT`. Because the level lives inside the integer, the resolution can be recovered from a bare cell ID with no side table — and a cell at one resolution maps deterministically to its ancestor at any coarser level, which is what makes hierarchical roll-up a pure integer operation rather than a re-encode of the raw points."
        },
        {
          "label": "Pick the coarsest resolution that still separates your data",
          "description": "Each finer level multiplies the number of distinct cells (and therefore `GROUP BY` cardinality) by the branching factor. Encode once at the finest resolution you'll ever need; roll up to coarser views in-query for free. The [Cookbook](#cookbook) and the resolution guide in the details below show the area-per-level trade-off."
        },
        {
          "label": "Cells are integers first, geometry on demand",
          "description": "Storage and joins use the compact integer. When you need to *draw* a cell, the boundary is materialized as `[lon, lat]` vertex pairs that pair with the [`spatial`](https://duckdb.org/docs/extensions/spatial) extension's polygon builders for GeoJSON output — the exact pattern is in the Cookbook."
        }
      ]
    },
    {
      "icon": "⬢",
      "title": "A5 vs H3 vs S2",
      "description": "All three are global cell-based spatial indices that produce integer IDs. The shape of the cell, and how uniform its area is across the globe, is what differs — and that difference, plus ecosystem maturity, is the whole decision.",
      "bulletPoints": [
        {
          "label": "Cell shape",
          "description": "[H3](https://h3geo.org/) tiles the world with hexagons (with 12 unavoidable pentagons at the icosahedral vertices). [S2](https://s2geometry.io/) uses curvilinear quadrilaterals derived from a cube projection. A5 uses **pentagons throughout**, derived from a [dodecahedral subdivision](https://a5geo.org/examples/teohedron-dodecahedron) — see [the A5 motivation page](https://a5geo.org/docs/motivation) for the geometric argument."
        },
        {
          "label": "Area uniformity",
          "description": "H3 hex area varies by roughly a factor of 2 across the globe — not a simple latitude effect, but a smooth pattern of 20 high-area hotspots and 12 low-area ones (the icosahedron's face centers and vertices), the way the panels on a soccer ball repeat. A5 cells are equal-area at each resolution by construction, which sidesteps that pattern entirely and matters when you're computing densities or comparing counts across cells that land in different parts of it. The [A5 vs H3](https://a5geo.org/docs/recipes/a5-vs-h3) page in the upstream docs walks through the trade."
        },
        {
          "label": "Neighbor count",
          "description": "Hexagons have a clean 6-neighbor structure that's nice for grid traversal. Pentagons have 5 edge-neighbors, which is slightly less convenient — but A5 has no \"pentagon exceptions\" the way H3 does, so neighborhood queries don't need special-case logic."
        },
        {
          "label": "Ecosystem maturity",
          "description": "H3 is older and has a much larger ecosystem (Postgres, BigQuery, Snowflake, Spark, plus Uber's first-party libraries). S2 is widely deployed inside Google. A5 is newer — the upstream library and specification are at [github.com/felixpalmer/a5](https://github.com/felixpalmer/a5). If you need the broadest tool support today, H3 is still the safest pick. Reach for A5 specifically when uniform cell area matters."
        }
      ]
    }
  ]
}
