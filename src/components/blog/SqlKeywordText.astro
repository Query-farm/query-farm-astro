---
interface Props {
  text: string;
}

const { text } = Astro.props;
const parts = text.split(/(\bLATERAL\b)/g);
---

{parts.map(part => part === 'LATERAL'
  ? <code class="font-mono text-[0.9em] tracking-normal">{part}</code>
  : part
)}
