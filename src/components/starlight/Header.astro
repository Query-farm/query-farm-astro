---
// Starlight Header override: render the site-wide Query.Farm header on docs
// pages so the docs sit inside the same shell as the rest of query.farm, plus a
// section subnav (breadcrumbs) beneath it — matching Haybarn / Orchard / Cupola.
import SiteHeader from '../layout/Header.astro';
import DocsSubNav from './DocsSubNav.astro';
---

<SiteHeader />
<DocsSubNav />
